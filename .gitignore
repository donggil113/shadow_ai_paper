__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/
# Public raw data is re-fetched by scripts/download_data.py; credentialed data
# (MIMIC-IV) must never be committed.
data/raw/
results/*.log
results/*.done
# LaTeX build artifacts
paper/*.aux
paper/*.bbl
paper/*.blg
paper/*.fdb_latexmk
paper/*.fls
paper/*.log
paper/*.out
paper/*.toc
paper/main.pdf
# unit-level prepared cohort files are never committed (pseudonymised person-level data)
results/korea_cohort_prepared.*
